import { HashedPassword } from '@account/domain';

export const mockHashedPassword1 = (): HashedPassword => new HashedPassword('HashedPas1');
export const mockHashedPassword2 = (): HashedPassword => new HashedPassword('HashedPas2');
